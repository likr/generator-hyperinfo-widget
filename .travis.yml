language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hyperinfo-widget' ]; then cd .. && eval "mv $currentfolder generator-hyperinfo-widget" && cd generator-hyperinfo-widget; fi

